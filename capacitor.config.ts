import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'TeLlevoAPP_Duoc',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
